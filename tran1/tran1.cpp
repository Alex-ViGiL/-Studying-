#include<iostream>
using namespace std;
int main()
{
    setlocale(LC_ALL, "Rus");

    /*
    int a1,a2,a3,a4,a5,a6;
   
    cout << "Номер " << endl;;

    cin >> a1 >> a2 >> a3 >> a4 >> a5 >> a6;

    if (a1 + a2 + a3 == a4 + a5 + a6)
    {
        cout << "Хорошо ";
    }
    else
    {
        cout << "Плохо ";
    }
    */

   /*
    int a, r, diametr, diagonal;

    cout << "Впишется ли квадрат в круг, если известны сторона квадрата и радиус круга?" << endl << "Введите радиус круга : ";
    cin >> r;
    cout << "Введите длину стороны квадрата : ";
    cin >> a;

    diametr = r * 2;
    diagonal = a * sqrt(2);

    if (diametr < diagonal) {
        cout << "Квадрат не помещается в круг ";
    }
    else {
        cout << "Квадрат помещается в круг";
    }
    */

    /*
        int a, b;
        cin >> a >> b;
        z
        if (a % b == 0) {
            cout << "Остаток от деления: 0";
        }
        else { cout << "Остаток от деления: " << a / b; };
   */
}